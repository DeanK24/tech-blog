<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Tech Blog</title>
        <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bulma@0.9.0/css/bulma.min.css">
        <link rel="stylesheet" href="/stylesheets/style.css">
        <script defer src="https://use.fontawesome.com/releases/v5.3.1/js/all.js"></script>
    </head>

    <body>
        <div class="wrapper">
            <header>
                <h1 class="has-text-black px-4 py-4 has-text-weight-bold">
                    Tech blog
                    <nav class="navbar mt-3">
                        {{#if loggedIn}}
                        <a href="/" class="navbar-item is-hoverable pt-1">Homepage</a>
                        <a href="/dashboard" class="navbar-item is-hoverable pt-2">Dashboard</a>
                        <a href="/dashboard/create/" class="navbar-item is-hoverable pt-1 mr-4">Create a Post</a>
                        <button id="logout" class="button is-danger">Logout</button>
                        {{else}}
                        <a href="/" class="button navbar-item is-hoverable pt-2">Homepage</a>
                        <a href="/login" class="button is-primary">Login</a>
                        {{/if}}
                    </nav>
                </h1>
            </header>
            <main>
                {{{ body }}}
            </main>
            <footer class="footer rows pb-3">
                <div class="row-is-full content has-text-centered">
                    <p>
                        <a href="/">Homepage</a>
                        {{#if loggedIn}}
                        |
                        <a href="/dashboard">Dashboard</a>
                        |
                        <a href="/dashboard/create/">Create a Post</a>
                        {{else}}
                        |
                        <a href="/login">Login</a>
                        {{/if}}
                    </p>
                </div>
            </footer>
        </div>
        {{#if loggedIn}}
        <script src="/javascript/logout.js"></script>
        {{/if}}
    </body>
</html>